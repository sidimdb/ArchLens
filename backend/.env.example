# Optional GitHub Personal Access Token.
# Required for analyzing PRIVATE repositories. For public repos only,
# it is still useful because it lifts the 60-requests-per-hour
# anonymous rate limit up to 5000/hour.
# Create at https://github.com/settings/tokens (scope: repo).
GITHUB_TOKEN=

# HTTP server port
PORT=8000
