# ArchLens Dashboard — environment configuration.
#
# Copy this file to `.env` (gitignored) and fill in real values.
#
# Vite exposes any variable prefixed with VITE_ to the browser bundle.
# Both of these are designed to be public — the security model is
# (1) RLS in Postgres + (2) Supabase Auth user sessions.

VITE_SUPABASE_URL=https://hfadouhihvcpsalxnyzf.supabase.co
VITE_SUPABASE_ANON_KEY=
