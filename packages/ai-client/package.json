{
  "name": "@archlens/ai-client",
  "version": "0.0.1",
  "private": true,
  "description": "Shared Anthropic / Claude client used by both archlens-statik (rule explanations + recommendations) and archlens-runtime (vision-based verify loop). Single source of truth for API key, model selection, and prompt-level helpers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2"
  }
}
