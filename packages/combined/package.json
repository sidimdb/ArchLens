{
  "name": "@archlens/combined",
  "version": "0.0.1",
  "private": true,
  "description": "ArchLens combined report — merges archlens-statik (architectural rules) and archlens-runtime (UX audit) outputs into a single project-health document.",
  "main": "dist/cli.js",
  "bin": {
    "archlens-combined": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
